/**
 * Environment variable helpers.
 *
 * NOTE: Zod-based env validation was temporarily disabled to debug a
 * deployment startup issue. The file is kept as a no-op stub so that
 * any leftover `import "@/lib/env"` keeps resolving cleanly. Re-enable
 * here once the deployment config issue is fully understood.
 */

// Intentionally empty for now. Add loadClientEnv() / loadServerEnv() helpers
// back here when ready to re-introduce validation.
export {};
